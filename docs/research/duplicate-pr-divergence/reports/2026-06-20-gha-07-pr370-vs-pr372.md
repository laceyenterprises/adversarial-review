# 2026-06-20 GHA-07 Duplicate PR Divergence: #370 vs #372

## Pair

- Repository: `laceyenterprises/adversarial-review`
- Ticket/theme: GHA-07, optional GitHub adapter read path
- Survivor: #370, branch `codex-gha-07/GHA-07`, pre-rescue head `9f103575c7269af84a678dd0922f425988b69ef1`
- Loser: #372, branch `codex-gha-07-a2cea167/GHA-07`, head `b9b4419b4ad0c37767489287d1acfbd9641c1939`
- Final loser disposition: close unmerged after the survivor carries the research report and strict-mode remediation.

## Decision

#370 is the stronger survivor. It already split the adapter work across focused commits, re-applied the reviewer anti-spoof allowlist after the first review, fixed adapter diff fallback after the second review, and documented the optional adapter contract in the SPEC. Its final remaining review findings were non-blocking but addressable, and the branch had a clean merge state.

#372 carried a broader one-commit implementation, but the final review found real fallback contract gaps: malformed adapter output could degrade `fetchState`, mergeability, and diff reads instead of falling back. Its remediation run then stalled before code changes because the mandatory fresh fetch timed out. That left #372 as useful comparison material, but not the best integration base.

## Comparison

| Area | #370 | #372 | Survivor read |
| --- | --- | --- | --- |
| Commit structure | Three focused commits: adapter reads, anti-spoof remediation, diff fallback remediation. | One large adapter commit plus unrelated-looking watcher/gate/test adjustments. | #370 was easier to audit and re-review. |
| Adapter fallback posture | Fallbacks were progressively tightened by review, including malformed diff and review-body anti-spoof paths. | Good try/catch pattern in many paths, but malformed snapshot/mergeability/diff output still had degraded-return holes. | #370 had fewer latent contract gaps after review. |
| Reviewer-body safety | Preserves local reviewer allowlist and head/state filtering for structured adapter results. | Final review did not focus here as the main blocker, but the branch was still wider and less remediated. | #370 carried the stronger anti-spoof evidence. |
| Discovery behavior | Documented env discovery and, after final rescue, discovers the superproject `../../modules/github-adapter/bin/github-adapter` path. | Review identified cwd-relative discovery as inert in the live service. | #370 now takes the stronger discovery idea and pins it with a test. |
| Operator approval/gate blast radius | Does not change gate approval defaults. | Changed default operator-approval label lookup behavior in `adversarial-gate-status.mjs`, a security-adjacent behavior shift needing extra proof. | Avoid the extra gate-surface blast radius in #372. |
| Telemetry/rate-limit concern | Leaves adapter telemetry as a documented follow-up surface rather than expanding scope during rescue. | Review surfaced adapter telemetry bypass concerns. | Capture as research/future work, not survivor-critical. |

## Salvage Folded Into #370

- Added the superproject auto-discovery depth that #372's review called out as a live-service gotcha.
- Hardened adapter binary auto-discovery before token handoff: discovered binaries must live under a trusted `modules/github-adapter` root, be regular executable files, be owned by the current uid or root, and not be group/other writable. Explicit operator env overrides remain available.
- Unified adapter and legacy head/state normalization through a shared helper so adapter-present and adapter-absent hosts do not diverge on state casing or optional fields.
- Removed the string-only review-body fast path. Without structured metadata, adapter review bodies now fall back to the legacy reader so local head/state filtering stays authoritative.
- Added tests for the real superproject discovery path, unsafe discovered binaries, and string-body fallback.
- Documented the trust checks and review-body structured-output contract in the SPEC.

## Final Remediation On Survivor

Final strict-mode remediation on #370 addressed every finding from the 2026-06-20 Claude review:

1. Auto-discovery missed the documented superproject layout.
   - Added `<repo-root>/../../modules/github-adapter/bin/github-adapter`.
   - Added focused coverage that builds the real relative depth.
2. Head/state normalizer divergence risk.
   - Added shared `normalizeHeadAndStateShape` and routed both adapter and legacy head/state paths through it.
3. String review-body fast path bypassed local filtering.
   - Removed the direct return for string-only adapter arrays and forced fallback to the legacy reader.
4. Token-bearing binary selected by bare existence.
   - Restricted auto-discovery to trusted roots and checked file type, owner, executability, and group/other write bits before passing tokens.

## Validation

- `node --test test/github-api.test.mjs test/adapters/subject-github-pr.test.mjs`: pass, 69 tests.
- `node --test test/watcher-title-guardrails.test.mjs test/adapters/operator-linear-triage.test.mjs test/adapters/operator-composite.test.mjs test/gemini-reviewer-routing.test.mjs test/kernel-contracts.test.mjs`: pass, 39 tests.
- `node --check src/github-api.mjs src/github-adapter-client.mjs src/cli.mjs src/watcher-title-guardrails.mjs src/adapters/operator/linear-triage/index.mjs`: pass.
- `npm ci`: pass; emitted the expected Node engine warning because this shell runs Node 26 while the package declares `>=20 <26`.

## Research Notes

This pair is useful because both branches chose the same high-level adapter-first/fallback-later design, but diverged on how much trust to place in the adapter process. #372 shows the failure mode of broad adapter adoption: catch blocks are easy to add, but malformed-success payloads can still silently degrade state. #370's review cycle shows the stronger pattern for this class of integration: local code must preserve authoritative normalization, head filtering, reviewer identity filtering, and credential handoff constraints even when the external adapter is preferred.
