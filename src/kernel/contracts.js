// Runtime-free bridge module for JSDoc and TS consumers of
// `contracts.d.ts`. Keeping a sibling `.js` file lets tools that
// follow import specifiers literally resolve `./contracts.js`.
export {};
