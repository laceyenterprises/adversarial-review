# GitHub — read access for PR polling
GITHUB_TOKEN=ghp_...

# GitHub bot PATs — write access to post reviews
# Used by codex-reviewer-lacey bot (reviews Claude/Clio PRs)
GH_CODEX_REVIEWER_TOKEN=ghp_...

# Used by claude-reviewer-lacey bot (reviews Codex PRs)
GH_CLAUDE_REVIEWER_TOKEN=ghp_...

# Optional routing pins.
# Leave unset for normal opposite-agent review routing.
# Set to codex to lock Claude out of first-pass reviews.
# ADVERSARIAL_REVIEW_DEFAULT_REVIEWER=codex

# Leave unset for the current codex remediation default.
# Set to claude-code only when Claude Code is approved for unattended remediation.
# ADVERSARIAL_REVIEW_DEFAULT_REMEDIATOR=codex

# AI provider keys
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Linear — for ticket status updates
LINEAR_API_KEY=lin_api_...

# Offline-period / quota-outage stale-state reaper thresholds (watcher startup).
# After a host outage (macOS upgrade + os-restart, a GitHub rate-limit storm, or
# any long watcher-down window) the reaper releases abandoned in-flight state so
# PRs re-review and AMA closers re-dispatch instead of wedging until manual
# rescue. Both default to 6h; lower only if you understand the live-work race.
# Age (ms) past which a `status='running'` reviewer pass is treated as a zombie.
# ADVERSARIAL_STALE_RUNNING_REVIEWER_PASS_MS=21600000
# Age (ms) past which a non-terminal AMA closer lease is released.
# ADVERSARIAL_STALE_CLOSER_LEASE_MS=21600000

# ── Poll-loop fairness + starvation signal (WPS-01) ──────────────────────────
# A backlog of PRs the loop cannot advance must never starve new-PR discovery.
# See docs/INCIDENT-2026-08-25-new-pr-discovery-starved-by-unadvanceable-prs.md.
# Wall-clock budget for the posted-review phase of one tick. Handlers cut off are
# deferred to the FRONT of the next tick, never dropped.
# ADVERSARIAL_WATCHER_POSTED_REVIEW_PHASE_BUDGET_MS=600000
# Per-handler deadline. A handler that never settles is abandoned so the tick can
# return to discovery; its side effects stay guarded by their own leases/CAS.
# ADVERSARIAL_WATCHER_POSTED_REVIEW_HANDLER_TIMEOUT_MS=300000
# How long ONE poll may stay in flight with a frozen poll_counter before the
# watcher pages itself. Defaults to max(15m, 3 x poll interval).
# ADVERSARIAL_WATCHER_POLL_STARVATION_MS=900000
# Consecutive observations of that state required before paging.
# ADVERSARIAL_WATCHER_POLL_STARVATION_CHECKS=3

# ASR-04 — the Argus security route. Default ON.
# A PR whose title carries no worker-class prefix cannot be routed to an
# adversarial reviewer, and a dependency bot can never produce one. With the
# route on, such a PR is ENQUEUED to data/argus-security-jobs/ instead of being
# written to a terminal `unroutable-bot-author` row -- the disposition that left
# adversarial-review#909 and #910 unreviewed for 14 hours.
# Set to 0 to roll back to that pre-ASR-04 terminal behaviour. Every terminal
# write it produces logs ARGUS_ROUTE_DISABLED, so a lever left flipped is never
# silent.
# ADVERSARIAL_ARGUS_SECURITY_ROUTE=0
