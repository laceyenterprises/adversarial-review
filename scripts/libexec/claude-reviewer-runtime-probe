#!/bin/sh
set -eu

if [ "$#" -ne 1 ]; then
  echo "usage: claude-reviewer-runtime-probe <uid>" >&2
  exit 64
fi

case "$1" in
  ''|*[!0-9]*|0)
    echo "invalid uid: $1" >&2
    exit 64
    ;;
esac

exec /bin/launchctl asuser "$1" /usr/bin/true
