{
  "name": "@arf/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ARF frontend screens (zero dependencies; renderers are pure functions over the ARF API payloads)",
  "engines": {
    "node": ">=23.4"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
